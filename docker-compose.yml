x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

services:
  loki:
    image: grafana/loki:3.0.0
    container_name: tfm-loki
    command: -config.file=/etc/loki/config.yml
    ports:
      - "3100:3100"
    volumes:
      - ./infrastructure/loki/config.yml:/etc/loki/config.yml:ro
      - loki-data:/loki
    networks:
      - tfm-network
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3100/ready || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    logging: *default-logging
    restart: unless-stopped

  promtail:
    image: grafana/promtail:3.0.0
    container_name: tfm-promtail
    command: -config.file=/etc/promtail/config.yml
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - ./infrastructure/promtail/config.yml:/etc/promtail/config.yml:ro
    networks:
      - tfm-network
    depends_on:
      loki:
        condition: service_healthy
    logging: *default-logging
    restart: unless-stopped

  grafana:
    image: grafana/grafana:11.0.0
    container_name: tfm-grafana
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GRAFANA_ALERT_WEBHOOK_URL: ${GRAFANA_ALERT_WEBHOOK_URL:-http://host.docker.internal:19093/alerts}
      GRAFANA_TELEGRAM_BOT_TOKEN: ${GRAFANA_TELEGRAM_BOT_TOKEN:-}
      GRAFANA_TELEGRAM_CHAT_ID: ${GRAFANA_TELEGRAM_CHAT_ID:-}
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./infrastructure/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./infrastructure/grafana/dashboards:/var/lib/grafana/dashboards:ro
    networks:
      - tfm-network
    depends_on:
      loki:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health | grep -q ok || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging: *default-logging
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v2.53.0
    container_name: tfm-prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --web.console.libraries=/usr/share/prometheus/console_libraries
      - --web.console.templates=/usr/share/prometheus/consoles
      - --web.enable-lifecycle
    ports:
      - "9090:9090"
    volumes:
      - ./infrastructure/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - tfm-network
    labels:
      observability.environment: ${OBS_ENVIRONMENT:-dev}
    depends_on:
      backend:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9090/-/ready || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    logging: *default-logging
    restart: unless-stopped

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    container_name: tfm-cadvisor
    privileged: true
    devices:
      - /dev/kmsg:/dev/kmsg
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    ports:
      - "8088:8080"
    networks:
      - tfm-network
    labels:
      observability.environment: ${OBS_ENVIRONMENT:-dev}
    logging: *default-logging
    restart: unless-stopped

  postgres:
    image: postgres:16.2-alpine
    container_name: tfm-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-records_db}
      POSTGRES_USER: ${POSTGRES_USER:-records_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    labels:
      observability.environment: ${OBS_ENVIRONMENT:-dev}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/db/postgresql/seed:/docker-entrypoint-initdb.d:ro
    networks:
      - tfm-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-records_user} -d ${POSTGRES_DB:-records_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    logging: *default-logging

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: tfm-backend
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-records_db}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-records_user}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      SPRING_JPA_HIBERNATE_DDL_AUTO: ${JPA_HIBERNATE_DDL_AUTO:-update}
      SPRING_JPA_SHOW_SQL: ${JPA_SHOW_SQL:-false}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-3600000}
      FLOWABLE_ASYNC_EXECUTOR_ACTIVATE: ${FLOWABLE_ASYNC_EXECUTOR_ACTIVATE:-true}
      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE: ${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE:-health,info,metrics,prometheus}
      MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS: ${MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS:-when_authorized}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:4200,http://localhost:4300}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      MAILING_ENABLED: ${MAILING_ENABLED:-true}
      MAILING_QUEUE_ENABLED: ${MAILING_QUEUE_ENABLED:-false}
      MAILING_FROM_EMAIL: ${MAILING_FROM_EMAIL:-no-reply@records.local}
      MAILING_FROM_NAME: ${MAILING_FROM_NAME:-Records}
      MAIL_HOST: ${MAIL_HOST:-mailpit}
      MAIL_PORT: ${MAIL_PORT:-1025}
      MAIL_SMTP_AUTH: ${MAIL_SMTP_AUTH:-false}
      MAIL_SMTP_STARTTLS: ${MAIL_SMTP_STARTTLS:-false}
      MAILING_VERIFICATION_BASE_URL: ${MAILING_VERIFICATION_BASE_URL:-http://localhost:4200/sede/verificar-email}
      MAILING_PASSWORD_RESET_BASE_URL: ${MAILING_PASSWORD_RESET_BASE_URL:-http://localhost:4200/sede/restablecer-contrasena}
      SIGNING_KEYSTORE_PASSWORD: ${SIGNING_KEYSTORE_PASSWORD:-changeit}
      STORAGE_DOCUMENTS_PATH: ${STORAGE_DOCUMENTS_PATH:-/app/data/documents}
    labels:
      observability.environment: ${OBS_ENVIRONMENT:-dev}
    ports:
      - "8080:8080"
    volumes:
      - ./backend/data/documents:/app/data/documents
    networks:
      - tfm-network
    depends_on:
      postgres:
        condition: service_healthy
      mailpit:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/api/v1/health/live || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging: *default-logging
    restart: unless-stopped

  mailpit:
    image: axllent/mailpit:latest
    container_name: tfm-mailpit
    ports:
      - "1025:1025"
      - "8025:8025"
    labels:
      observability.environment: ${OBS_ENVIRONMENT:-dev}
    networks:
      - tfm-network
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8025/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging: *default-logging
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: ./front-end/Dockerfile
    container_name: tfm-frontend
    labels:
      observability.environment: ${OBS_ENVIRONMENT:-dev}
    networks:
      - tfm-network
    expose:
      - "80"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:80/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging: *default-logging
    restart: unless-stopped

  backoffice:
    build:
      context: .
      dockerfile: ./back-office/Dockerfile
    container_name: tfm-backoffice
    labels:
      observability.environment: ${OBS_ENVIRONMENT:-dev}
    networks:
      - tfm-network
    expose:
      - "80"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:80/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging: *default-logging
    restart: unless-stopped

  nginx:
    image: nginx:1.25-alpine
    container_name: tfm-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./infrastructure/nginx/certs:/etc/nginx/certs:ro
      - ./infrastructure/nginx/www:/var/www/letsencrypt:ro
      - nginx-cache:/var/cache/nginx
    networks:
      - tfm-network
    depends_on:
      - frontend
      - backoffice
      - backend
      - grafana
      - prometheus
      - loki
      - mailpit
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:80/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging: *default-logging
    restart: unless-stopped

networks:
  tfm-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
  loki-data:
    driver: local
  grafana-data:
    driver: local
  prometheus-data:
    driver: local
  nginx-cache:
    driver: local
